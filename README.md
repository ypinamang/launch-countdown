# launch-countdown
Implementation of a countdown timer that starts at n seconds. Uses async/await with a Promise to implement visible countdown. 

The input field and button for entering the initial value are hidden during countdown so that the user can only see the countdown.


